
@ComponentType(initial = "<%- initial %>", name = "org.bip.spec.<%- name %>")
public class <%- name %> {
private Logger logger = LoggerFactory.getLogger(<%- name %>.class);
private   JSONLogger loggerJSON = JSONLogger.getLogger();
